{

		var first, second, i, tmp: int;
		first=1 && 0;
		print(first);
		first=1 && 1;
		print(first);
		first=0 && 1;
		print(first);
		first=0 && 0;
		print(first);
		first=1 || 0;
		print(first);
		first=1 || 1;
		print(first);
		first=0 || 1;
		print(first);
		first=0 || 0;
		print(first);
		first=0&&(first+4)&&5||0;
		print(first);





}
